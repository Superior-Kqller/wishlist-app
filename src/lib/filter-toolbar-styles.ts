/**
 * Общие классы для controls в панели инструментов.
 *
 * Высота `h-10` задана здесь и только здесь: в панели раньше соседствовали 48,
 * 36 и 32 пикселя одновременно, и у ряда не было общего ритма. Единственный
 * элемент, которому позволено быть выше, — ничего; поиск тоже равняется по
 * этой высоте.
 */
export const filterBarTriggerClass =
  "h-10 border-border/85 bg-[hsl(var(--surface-3)/0.85)] shadow-none hover:border-primary/32 hover:bg-[hsl(var(--surface-4)/0.85)]";
